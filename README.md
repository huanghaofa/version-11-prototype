# P0 原型中心 GitHub Pages 部署包

本目录已可直接作为 GitHub Pages 仓库根目录。

1. 将本目录中的 `index.html`、`.nojekyll`、`prototypes/` 等内容上传到仓库根目录。
2. 在 GitHub 仓库进入 `Settings → Pages`。
3. 选择 `Deploy from a branch`，分支选择 `main`，目录选择 `/(root)`。
4. 保存后等待 GitHub 生成 Pages 地址。

不要只上传 ZIP，也不要在仓库根目录外再套一层同名文件夹。站点仅含 Mock 数据。
