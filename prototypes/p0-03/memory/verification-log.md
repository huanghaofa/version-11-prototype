# 验证记录

## 最新状态

- Overall: Passed
- Last verified: 2026-08-19 17:09 CST
- Environment: 本地 HTTP `127.0.0.1:8094`，Codex 内置浏览器

## Step 01：静态与资源检查

- JavaScript：`js/*.js`、`mock/*.js`、`annotations/*.js` 全部通过 `node --check`。
- JSON：`config/nav.json`、`config/workflow.json` 解析通过。
- 页面引用的 CSS、JS、JSON 本地资源存在。
- 最终复验请求返回 200/304，无新增本地资源 404；初测 favicon 404 已通过 data favicon 修复。

## Step 02：卡券补贴数据

- 两页导航切换通过。
- 首屏共 6 条 Mock，一页 5 条并可翻至第二页；一张卡券一行。
- 核销码中间四位脱敏；VIN、手机号、oneID 分列。
- 展开更多、重置、查询和无结果状态通过。
- 部分退款明细验证：预计金额保留，实际金额显示“—”。
- 全额退款明细验证：订单状态“已全额退款”、结算状态“无需结算”、预计金额 `¥0.00`、实际金额“—”。
- 线上商品、线下备件明细均由 Mock 当前完整快照展示，页面无金额计算入口和汇总卡片。

## Step 03：补贴规则设置

- 查询、重置通过。
- 新增规则后列表出现；编辑后名称更新；未关联规则二次确认后可删除。
- 已关联规则点击删除时出现现网限制提示，记录未删除。
- 查看弹窗中线上、线下配置均可见；精确检查“结算渠道”表单字段数量为 0。

## Step 04：响应式与稳定性

- 1280×720 桌面布局截图复核通过。
- 390×844 窄屏布局：body、主内容、侧栏宽度均为 390px，页面级无横向溢出；宽表格在卡片内部滚动。
- 浏览器 error/warn 日志为空。
- 标注在两页均生成，标注开关已移至右下角避免遮挡品牌和导航。

## 已知验证边界

- 浏览器安全策略不允许自动打开 `file://`，因此“双击直开”仅完成代码路径审查：导航 fetch 失败后会使用相同的内置菜单配置；HTTP 完整交互已通过。
- 当前环境未安装 Mermaid CLI，流程图源文件未做本地渲染；已确认第一行是 `flowchart TD`、没有代码围栏，适合直接粘贴到飞书 Mermaid。
- 正式接口、权限、金额精度、乱序及重试策略不在静态原型验收范围。
