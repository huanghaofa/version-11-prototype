# 验证记录

## 最新状态

- Overall: Pass
- Last verified: 2026-07-23 09:57
- Entry: `http://127.0.0.1:8080`

## 2026-07-23 规则渠道运行时路由验证

- 工具：`prototype-verifier`、`systematic-debugging`、Playwright 浏览器自动化、JavaScript 语法检查、截图目检。
- 验证动作：检查规则列表 9 列、运行时路由图、规则新增/编辑弹窗、线上和线下配置显隐；执行新建、双配置编辑、删除；检查实例详情的线上/线下路由表达及 390px 移动端布局。
- 通过：规则列表不存在“结算渠道”列，新增/编辑弹窗不存在结算渠道字段或渠道枚举说明。
- 通过：新增和编辑时 1 个线上配置区、3 个线下配置区始终同时可见；页面明确提示两套配置均需维护。
- 通过：新建测试规则后线上、线下配置同时保存；分别修改为 75% 和 65% 后列表正确更新；删除后恢复原 2 条规则。
- 通过：规则页展示“读取核销渠道 → 结算执行时判断 → 线上使用线上配置 / 线下使用线下配置”的运行时路由。
- 通过：线上实例详情显示自动选择线上配置，线下实例详情显示自动选择线下配置。
- 首次失败：390px 规则页全局宽度为 559px，分类为响应式布局问题；最小失败范围为运行时路由图的固定最小宽度。
- 修复：移动端将运行时路由图改为纵向布局，清除三个节点的最小宽度并旋转连接箭头。
- 复验通过：390px 规则页和规则弹窗全局宽度均为 390px；规则表只在自身容器滚动，规则弹窗内部纵向滚动。
- 通过：JavaScript 语法无错误；浏览器无 console error；本地资源无 4xx/5xx；标注运行时正常。
- 证据：`artifacts/settlement-rules.png`、`artifacts/settlement-rule-form.png`、`artifacts/instance-detail.png`、`artifacts/offline-instance-detail.png`。
- 结论：pass。

## 2026-07-23 商品适用性与用户字段精简验证

- 工具：`prototype-verifier`、Playwright 浏览器自动化、JavaScript 语法检查、截图目检。
- 验证动作：核对 17 列实例报表、11 列 E3S 提交记录；检查混合适用性订单；打开线上和线下详情；回归渠道/来源筛选、规则新增弹窗、390px 移动端和标注运行时。
- 通过：实例报表和 E3S 提交记录均无“归属类型”列，报表无对应筛选项；VIN、手机号码、oneID 三列保留。
- 通过：混合订单共 3 件商品，其中 2 件适用、1 件不适用；不适用商品明确标注原因，分摊优惠为 ¥0.00。
- 通过：适用商品商城价合计为 ¥528.00，等于 ¥399.00 + ¥129.00，不包含不适用商品 ¥259.00。
- 通过：适用商品网点价合计为 ¥502.00，等于 ¥380.00 + ¥122.00，不包含不适用商品 ¥246.00。
- 通过：适用商品实际优惠为 ¥100.00，等于 ¥80.00 + ¥40.00 - ¥20.00；结算基数 ¥100.00 × 80% = 预计补贴 ¥80.00。
- 通过：详情说明“先过滤适用商品”，适用/不适用样式和原因清晰；截图目检无文字遮挡。
- 通过：线上/商城 5 条、线下/E3S 1 条筛选正确；规则四个价格来源不变；390px 无全局水平溢出；浏览器无 console error，资源无 4xx/5xx。
- 证据：`artifacts/settlement-report.png`、`artifacts/instance-detail.png`、`artifacts/offline-instance-detail.png`、`artifacts/settlement-rule-form.png`、`artifacts/mobile-report.png`。
- 结论：pass。

## 2026-07-23 核销渠道、来源和价格快照全局验证

- 工具：`prototype-verifier`、Playwright 浏览器自动化、JavaScript 语法检查、截图目检。
- 验证动作：检查报表 18 个表头和 6 条实例；分别筛选线上、线下、商城、E3S；打开线上与线下详情；打开规则新增弹窗；切换 390px 移动端视口；检查资源响应、控制台和标注运行时。
- 通过：5 条“线上 / 商城”和 1 条“线下 / E3S”样例均正确落列，渠道和来源筛选结果分别为 5 条和 1 条。
- 通过：报表展示商城商品单价/合计、实际优惠金额、卡券面值、网点价、价格标准类型、结算基数和结算金额；商城记录的卡券面值来源为卡券中心，线下记录的商城商品价显示不适用。
- 通过：线上详情展示四项价格快照并突出“实际优惠金额”，计算来源为商城；线下详情突出“网点价”，计算来源为 E3S。
- 通过：规则新增弹窗展示四个线上价格标准；商城商品单价、实际优惠金额、网点价均标记“商城传入”，卡券面值标记“卡券中心读取”。
- 通过：桌面截图目检无文字遮挡；390px 下无全局水平溢出，报表只在表格容器内横向滚动，详情价格卡片为两列。
- 通过：JavaScript 语法无错误；浏览器无 console error；本地资源无 4xx/5xx；标注开关保持单例，空标注数据不生成标注点。
- 证据：`artifacts/settlement-report.png`、`artifacts/instance-detail.png`、`artifacts/offline-instance-detail.png`、`artifacts/settlement-rule-form.png`、`artifacts/mobile-report.png`。
- 结论：pass。

## STEP-01 实例报表与金额口径

- 工具：`prototype-verifier` + Chromium 自动化检查。
- 验证动作：核对实例表 12 个表头和 5 条数据；检查 VIN 行只填写 VIN 列，用户行分别填写手机号码和 oneID 列；切换 390px 视口检查表格容器滚动。
- 通过：主表渲染 5 条唯一卡券实例，优惠、退款扣减、有效优惠和应结算金额正常。
- 通过：其中 3 条为 VIN 归属、2 条为手机号 + oneID 归属；按归属对象筛选后分别显示 3 条和 2 条。
- 通过：归属信息已拆为“归属类型、VIN、手机号码、oneID”四列。
- 证据：`artifacts/settlement-report.png`
- 结论：pass。

## STEP-02 实例详情与退款重算

- 通过：退款窗口内展示预计金额；订单冻结后展示最终金额、E3S 请求 ID 和自动提交证据。
- 通过：VIN 详情展示 `LGBH52E07RY123456`；用户级详情同时展示 `186****3046` 和 `OID-000284917`。
- 证据：`artifacts/instance-detail.png`
- 结论：pass。

## STEP-03 E3S 订单级提交

- 通过：实例报表中无 checkbox、无生成批次按钮、无确认弹窗。
- 通过：E3S 提交记录页渲染 3 条订单级记录，每条均包含 `REQ-E3S-` 唯一请求 ID、订单号、实例 ID、结算归属对象和结算金额。
- 通过：E3S 提交记录同样按“归属类型、VIN、手机号码、oneID”四列展示，VIN 与用户级数据落列正确。
- 证据：`artifacts/e3s-submissions.png`
- 结论：pass。

## STEP-04 卡券规则设置原页面扩展

- 工具：`prototype-verifier` + Chromium 自动化 DOM、截图和响应检查。
- 验证动作：打开“卡券规则设置”，核对 10 个表头和 2 条规则；检查线上、线下、线上+线下三个枚举和表单分区显隐；执行新增、编辑、删除及核销锁定回归；切换 390px 视口检查页面和弹窗布局。
- 通过：导航、页面标题和实例详情入口均使用“卡券规则设置/查看结算规则”，不存在“结算规则调整”或“调整原规则”。
- 通过：规则 ID、模板 ID、模板名称查询和重置有效；导出提供成功反馈。
- 通过：结算渠道下拉仅包含线上、线下、线上+线下；默认线上只显示线上规则，线下只显示 3 个线下配置区，线上+线下同时显示两类配置。
- 通过：线上结算价格标准值默认选中“实际优惠金额”，列表对应展示“实际优惠金额”，未出现商城专属金额来源字段。
- 通过：新建线上+线下规则后列表由 2 条变为 3 条；编辑为线上后线下规则和价格标准值显示“—”；删除后恢复为 2 条。
- 通过：已有 18 条核销记录的规则禁止编辑和删除，均显示明确提示。
- 通过：桌面端无全局溢出；390px 视口无全局溢出，规则表仅在自身容器内横向滚动。
- 证据：`artifacts/settlement-rules.png`、`artifacts/settlement-rule-form.png`
- 结论：pass。

## 全局验证

- JavaScript 语法和导航 JSON：通过。
- 本地资源：无 4xx/5xx。
- 控制台：无 error。
- 核心链路：确认 VIN 或手机号 + oneID 归属 → 订单冻结 → 自动提交一次 E3S → 查看提交结果，通过。
- 移动端：390px 视口无全局水平溢出；实例报表仅在自身容器内横向滚动。
- 标注运行时：开关单例，空数据下无标注点。
- 结论：pass。
