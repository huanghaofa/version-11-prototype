# 项目决策记录

## D-001 独立静态后台原型

使用 HTML、CSS 和原生 JavaScript建立独立原型；原始 Axure 仅作为页面结构和已有事件证据，不直接修改。

## D-002 事件命名

保留业务原话“用户订单消费金额达N元”；正式行为编码未确认，因此展示“待分配”。

## D-003 默认判断口径

后台默认按支付成功后的订单实付金额判断是否达到 N；金额口径与触发时点均保留为可选项，等待正式评审确认。

## D-004 页面范围

本期只交付活动中心后台行为事件配置页。商城下单、自动跳转、抽奖、领奖与核销前端均不制作。
