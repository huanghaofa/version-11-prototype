# 待确认事项

- 新增“部分叠加互斥”对应的真实枚举值，以及两组券 ID 字段的接口命名。
- 从新增方式切换回历史单一方式时，未生效的另一组名单是保留草稿还是清空。
- 批量导入的正式模板字段、文件格式、单次行数/大小上限、异步任务与结果保留时长。
- 指定券列表是否还需要导出历史和跨页选择上限。
- 非新商城用券场景是否复用相同优先级。
- 正式权限、操作日志和发布后可编辑范围。
- “同一目标券不可同时配置两类关系”为新增方式的防冲突设计，需产品/研发最终确认。
