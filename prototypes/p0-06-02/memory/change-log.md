# 项目变更记录

## 2026-07-17

- 创建项目基础框架。
- 完成步骤01：建立390 × 844移动端框架、真实客户端状态栏与返回图标资产、两辆绑定车辆和五个活动状态模型。
- 完成步骤02：实现SA来源提示、默认VIN、三级活动分区、默认勾选、已参与/需认证置灰和VIN切换重算。
- 完成步骤03：实现活动复选、底部选择数、一键批量执行、自动抽奖、直接发券和统一结果反馈。
- 完成步骤04：实现去绑车/去认证离开提示、功能说明、交付README、视觉对照与标注按钮避让。
- 完成步骤05：新增SA选择活动页、全选/逐项选择、二维码生成结果、sceneId与活动冻结快照。
- 以真实PNG二维码资产替代视觉占位，并串联“模拟用户扫码”到既有客户聚合页。
- 客户聚合页改为读取本次生成的活动ID集合；SA选择四项时，扫码页只展示四项。
- 更新README、需求、决策、验收映射、交互说明与视觉QA证据。
- 完成步骤06：新增“我的SA数据”概览/活动/卡券三页签，活动列表可下钻活动详情与参与记录详情，卡券列表可下钻卡券详情与简版生命周期。
- 移动端报表按当前SA隔离，手机号码和VIN脱敏，不展示用户统一标识，不提供导出入口。
- 完成步骤07：新增二维码重新生成确认、新码生效/旧码失效、二维码过期、SA无可投活动和活动提交前下线的部分未处理状态。
- 扩展为12个主演示路由，并以查询状态补充二维码过期页；保留原SA选活动、生成码、客户聚合页和一键参与主路径。
- 完成步骤09：SA活动卡片新增独立详情入口，展示基本信息、活动规则、关联卡券、关联权益和互斥关系。
- 完成步骤10：用户聚合页新增互斥组单选、自动切换提示、提交复核及“互斥已参与”锁定状态。
- 完成步骤11：移动端活动标签统一为“领券/抽奖”，卡券报表统一为“已核销/核销率”。
- 完成步骤12：SA侧切换为蓝色主视觉，用户聚合页保留红色；活动详情移除主办方并将字段改为领券方式；SA卡券状态统一为已生效。

## 2026-08-10

- 组合活动增加原名称、展示名称和结构化适用门店；SA全链路优先展示新名称，空值回退原名。
- SA活动池增加独立投放、组合状态、当前SA门店三项交集过滤；未命中门店的回退样例不进入列表或二维码快照。
- 明确组合活动适用门店只控制SA查看/分享，不修改子活动与卡券核销门店。
- 完成步骤13：新增组合活动对象，SA选择页和用户聚合页均只展示一张大活动卡片。
- 新增组合活动未绑车状态：卡片可见、卡券隐藏、复选框禁用并提供去绑车入口。
- 绑车后在原卡片内按当前VIN展示专属卡券和权益；切换两辆VIN后内容动态刷新，不展示人群标签。
- 组合活动参与状态按“组合活动ID + VIN”分别记录；一个VIN参与不影响另一个VIN。
- 完成步骤14：生成和重新生成前新增二维码有效期抽屉，后台默认30分钟预选，可选范围5分钟至1天，取消不生成。
- 二维码结果页新增有效时长、动态失效时间和配置版本快照。
- 新增`activity-aggregation-unbound`评审路由和组合活动/时长业务标注。
- 静态资源版本升级为`20260810-1`。
- 完成步骤15：确认移动端主入口不依赖fetch或本地接口，可直接通过file协议运行。
- README、协作规则和项目记忆改为直接双击index.html预览，HTTP仅作为可选调试方式。
- 接入上级统一静态入口`SA动态二维码原型_静态预览_20260810.html`。
- 完成步骤16：组合活动VIN匹配结果支持同时命中多个子活动。
- 默认VIN现演示同时命中两个子活动；用户端仍为一张大卡片，卡内汇总展示两类卡券和全部权益。
- 一键参与结果明确“命中2个子活动，已全部发放”，组合活动仍按“组合活动ID + VIN”只记一次参与。
- 静态资源版本升级为`20260810-2`。
- 完成步骤18：组合活动未绑车时不再渲染“卡券内容待解锁”占位区。
- 未绑车状态仅保留活动基本信息、禁用勾选状态和去绑车入口；已绑车卡券汇总展示不受影响。
- 静态资源版本统一升级为`20260810-4`。

## 2026-08-14

- 完成步骤19：新增SA活动分享海报编辑与预览，固定嵌入二维码、SA姓名/工号、门店和有效期。
- 海报支持编辑标题、副标题和扫码引导语，提供恢复默认、继续编辑、保存相册和分享反馈。
- 海报来源增加系统模板/SA上传自定义底图两种方式；上传JPG/PNG且不超过10MB，仍叠加标准二维码与来源信息。
- 完成步骤20：二维码有效期增加“长期有效（至活动有效期）”；聚合码截止到冻结活动中最晚结束时间。
- 二维码快照补充durationMode口径；长期有效不改变各活动实时状态过滤。
- 静态资源版本升级为`20260814-1`。
- 完成步骤21：从海报编辑和最终预览共用组件移除客户不应感知的“二维码与活动范围已锁定”内部状态文案。
- 活动范围冻结逻辑保持不变，仅不再在客户海报上展示；静态资源版本升级为`20260814-2`。
- 完成步骤22：用户扫码聚合页移除号码级、绑车级、认证级分区标题，全部活动改为连续列表。
- 绑车/认证升级提示同步隐藏活动等级术语；SA选择页与内部三级准入逻辑不变；静态资源版本升级为`20260814-3`。
