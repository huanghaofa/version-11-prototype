# 未确认项

- 正式页面的品牌标题、WebView胶囊按钮和客户端状态栏由宿主App提供，本原型仅模拟现有样式。
- 正式环境抽奖结果动效与卡包跳转文案待UI阶段确认，不阻塞当前流程原型。
- 动态二维码有效时长由后台配置候选项和默认值；固定时长范围5分钟至1天，并增加“长期有效（至活动有效期）”；原型默认仍为30分钟。
- SA活动列表正式默认勾选策略尚未单独配置；原型暂采用全部勾选，SA可全选或逐项取消。
- SA移动端自定义查询区间正式上限仍以最终配置为准；原型提供近7日/近30日演示切换，不提供导出。
- 同一VIN同时命中多个组合子活动的规则已确认：全部执行，不阻断、不记异常，不设优先级和顺序。
