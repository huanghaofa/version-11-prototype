# 待确认事项

以下事项不阻塞原型，统一以“待研发确认”标注：

- oneID 返回默认卡及 VIN 的正式字段名、失败码和超时策略。
- 积分系统发放、查询、对账和冲正接口的正式字段、状态码和错误码。
- 卡券中心业务主场景、业务子场景的正式字段名、枚举编码与同步机制；原型当前按已核对的卡券枚举写死。
- 预算余额/已用预算查询接口的正式字段、刷新频率和超时降级策略。
- 被隐藏的成本中心字段是否仍为积分接口必传，以及默认值由哪个系统提供。
- 对账采用实时接口、日终接口还是文件交换。
- 正式审核角色、补发权限和冲正权限。
