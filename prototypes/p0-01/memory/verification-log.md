# 验证记录

## 2026-07-20 全局验证

- 验证工具：Node 语法检查、本地 HTTP、浏览器自动化。
- JavaScript：`js` 页面模块与 `mock/data.js` 全部通过语法检查。
- 资源：入口、导航配置、后台页脚本、用户端脚本、标注运行时均返回 HTTP 200。
- 入口与导航：原型说明、卡券列表、新建兑换券、查询与兑换、系统接口与边界均可进入。
- 后台：商城营销/新商城/兑换券字段存在；商品弹窗可选择多个 SKU、调整同 SKU 数量；修改后总件数联动为 4。
- 用户端：初始无默认 VIN，下一步禁用；选择第二辆车后进入确认页；确认后展示履约成功和卡券已核销。
- 列表：8 个 SIT 基线列完整渲染。
- 接口页：3 方职责矩阵、6 步成功链路、6 条接口建议完整渲染。
- 视觉：桌面端成功页截图检查通过，手机框与说明区无明显遮挡或溢出。
- 连续失败次数：0（验证中发现总件数未联动和 VIN 默认选中，均修复后复验通过）。
- 结论：pass。

## 2026-07-21 卡券状态文案校验

- 成功页“卡券状态”已改为“已核销”。
- 前台卡券中心统计和筛选标签同步改为“已核销”。
- 页面脚本语法检查通过；业务页面无“已使用”残留。
- 结论：pass。

## 2026-07-21 已核销卡券回看与虚拟权益验证

- 步骤：Step 08 已核销卡券兑换详情。
- 验证工具：Node语法检查、本地HTTP、浏览器DOM交互、页面截图和console日志。
- 入口：我的卡券展示已核销兑换券及“查看内容”，点击成功进入兑换详情。
- 兑换内容：展示车机流量包10GB与智能座舱主题皮肤、各自数量、车联网履约和权益生效说明。
- 履约快照：展示车辆、车牌/VIN、履约状态、权益生效时间、兑换单号、兑换时间和卡券已核销状态。
- 只读边界：页面无修改、再次兑换、撤销、退款或恢复入口；返回我的卡券路径通过。
- 资源与运行：新增页面、我的卡券、Mock数据和导航配置均HTTP 200；console warning/error为空；视觉截图无明显遮挡。
- 连续失败次数：0。
- 结论：pass。

## 2026-07-20 本地直开兼容修复

- 问题：`file://` 页面不能通过 `fetch` 读取 `config/nav.json`，导航未初始化。
- 修复：增加 `config/nav.js` 内嵌配置；`file:` 协议直接使用内嵌配置，HTTP 模式仍优先使用 JSON，并保留失败回退。
- 静态验证：`config/nav.js`、`js/nav.js` 语法检查通过，脚本加载顺序及 `file:` 分支引用检查通过。
- 自动浏览器限制：安全策略不允许自动化工具导航到 `file://`，因此需在用户当前标签刷新后完成最终肉眼确认。

## 2026-07-21 N选M与完整前台验证

- 步骤：Step 06 后台N选M规则、Step 07 完整前台原型。
- 验证工具：Node语法检查、本地HTTP、浏览器DOM与交互检查、页面截图、console日志。
- 后台通过项：兑换方式默认N选M；候选商品数N=5；每券可兑件数M=2；同SKU最多2件；5个候选商品均有履约方式下拉；下拉当前只有“车联网履约”一个枚举。
- 前台入口通过项：卡券中心展示5选2兑换券，可从卡券卡片进入券详情。
- 商品详情通过项：券详情及选品页均有商品详情入口；详情展示SKU、价格、库存、履约方式和说明，关闭后选品状态保留。
- 5选2通过项：验证同一SKU数量2后下一步启用；验证两个不同SKU各1件后下一步启用；达到2件后所有增加按钮禁用。
- 兑换主链路（已被本日后续VIN绑定规则更新）：当时验证了选品→选VIN→确认→成功；当前版本以“券实例VIN自动带入”为准，见后续验证记录。
- 资源与运行：入口及4个关键脚本/配置均HTTP 200；console warning/error为空；视觉截图无明显遮挡。
- 连续失败次数：0。
- 结论：pass。

## 2026-07-21 券实例VIN与零库存限制验证

- 步骤：Step 09 券实例VIN与零库存限制。
- 验证工具：Node语法检查、本地HTTP预览、浏览器DOM交互、页面截图和console日志。
- VIN绑定：卡券列表和券详情均展示绑定车辆；兑换流程由5步缩减为4步，页面无选车步骤、无`data-vin`控件。
- 直接确认：同一SKU选择2件后，“选好了，确认兑换”直接进入确认页；确认页只读展示日产轩逸、车牌及`LGBH52E02RS019375`。
- 零库存：在线音乐会员月卡展示“已售罄”，数量增加按钮为禁用状态，数量保持0且不计入已选件数；商品详情入口仍保留。
- 成功链路：确认兑换后车联网履约成功，卡券状态展示“已核销”。
- 运行结果：页面脚本语法通过；浏览器warning/error为空；选品页截图未见阻塞性交互或明显布局问题。
- 连续失败次数：0。
- 结论：pass。

## 2026-07-22 兑换即核销与三类履约全局验证

- 步骤：Step 10 全商城商品与履约配置、Step 11 后台兑换券履约表单、Step 12 前台履约查询、Step 13 规则与接口同步。
- 验证工具：Node 语法检查、本地 HTTP 预览、浏览器 DOM 与交互检查、默认视图及 900px 窄屏截图、console 日志。
- 语法与配置：所有页面脚本、Mock、导航和标注脚本通过语法检查；导航 JSON 解析通过。
- 后台建券：候选池包含车联网商品、精品实物和门店服务；履约下拉按商品展示车联网履约或直邮/到店核销；供应商下拉包含新商城三条供应商数据，筛选“日产精品生活馆”后仅显示对应商品。
- 后台履约单：新增“兑换券履约”菜单；5项查询条件齐全；直邮筛选正确；履约单详情展示商品状态、供应商、物流单号与3条物流轨迹。
- 前台兑换：5选2候选商品含三类履约及零库存商品；零库存增加按钮禁用；验证选择“日产舒适头枕 + 基础洗车服务”，确认页同时展示直邮地址和到店核销说明。
- 兑换即核销：确认兑换后成功页立即展示“卡券已核销”和“履约/发货处理中”，可进入兑换单履约页。
- 履约状态：历史兑换单展示车联网待履约/履约中/履约成功/失败、直邮已发货/失败、到店待核销；失败商品均有联系客服入口。
- 物流与失败交互：直邮“查看物流”展示顺丰单号及轨迹；失败商品弹窗明确提示联系客服且卡券仍为已核销。
- 响应式：默认视图和900px窄屏均无页面横向溢出；手机框和履约卡片可见，窄屏主内容单列展示。
- 运行日志：浏览器 warning/error 为空。
- 连续失败次数：0（验证发现初始候选池仍引用旧5个车联网SKU，已改为跨三类履约候选池后复验通过）。
- 结论：pass。

## 2026-07-22 商品选择分页与全选验证

- 步骤：Step 14 商品选择全选与分页。
- 验证工具：Node 语法检查、本地 HTTP 预览、浏览器 DOM 与实际点击、窄屏截图、console 日志。
- 默认分页：商品池共 9 条，每页 5 条；第一页展示 5 条，第二页展示 4 条；上一页、下一页、页码和当前页信息正确。
- 全选本页：默认跨页已选 5 个商品；第一页执行全选后，本页 5 条全部勾选，累计已选数量由 5 更新为 7。
- 跨页保留：进入第二页再勾选 `SKU-20002`，累计数量更新为 8；返回第一页后 5 条仍全部勾选。
- 筛选保留：筛选供应商“日产精品生活馆”后仅展示 2 条商品，累计已选数量仍为 8；重置筛选后恢复共 9 条。
- 提交汇总：提交后弹窗关闭，候选商品数 N 与候选池条目数均更新为 8，证明所有分页选择已汇总。
- 视觉与运行：637px 宽视图下弹窗未超出视口宽度，表格可横向滚动；浏览器 warning/error 为空。
- 连续失败次数：0。
- 结论：pass。

## 2026-07-28 单一履约、选店核销码与商城确认订单验证

- 步骤：Step 15 单一履约与商品级兑换上限、Step 16 到店核销与直邮商城下单。
- 验证工具：Node 语法检查、导航 JSON 解析、本地 HTTP 预览、浏览器 DOM 与实际点击、页面截图、console 日志。
- 后台单一履约：整券履约方式下拉包含车联网履约、直邮、到店核销；从车联网切换直邮时弹窗列出 5 个不兼容商品，确认后仅移除不兼容商品。
- 商品筛选与上限：直邮商品弹窗仅返回 `SKU-20001`、`SKU-20002`；表格保留供应商筛选、本页全选和分页控件；每条商品独立展示“单商品最多可兑”输入，主表单无全局上限。
- 三张示例券：前台卡券中心分别展示车联网5选2、精品实物2选1直邮、门店养护2选1，页面明确三者为独立券而非同券履约切换。
- 到店链路：未选省、市、门店时下一步禁用；依次选择广东省、深圳市、深圳东风南方华侨城店后才可选品；选择基础洗车服务后生成核销码。
- 到店状态拆分：核销码页同时展示“卡券已核销”和“门店服务状态：待核销”，并展示门店、商品、核销码及前置条件。
- 直邮链路：选择车载香氛礼盒后进入新商城确认订单；页面显示 `couponId=EX202607200002&productIds=SKU-20002`，可选择现有地址或新增并使用地址。
- 直邮成功顺序：模拟失败提示“未创建兑换单，卡券仍可使用”；提交成功后回跳兑换详情，展示商城订单号、地址、卡券已核销及发货状态待发货。
- 车联网商品级上限：`SKU-10001` 最大数量为2，可连续增加到2；其他候选商品上限为1；零库存 `SKU-10005` 的增加按钮禁用。
- 资源与运行：入口、样式、Mock、导航及新增页面脚本均由浏览器成功加载；浏览器 warning/error 为空；选品页截图未见明显遮挡或横向溢出。
- 历史替代说明：2026-07-22 验证记录中的“同券跨三类履约、按商品选择履约方式”已被本轮单一履约规则替代。
- 连续失败次数：0。
- 结论：pass。

## 2026-07-28 选门店页只读 VIN 验证

- 步骤：Step 17 选门店页展示只读 VIN。
- 验证工具：Node 语法检查、本地 HTTP 预览、浏览器 DOM 与实际下拉交互、页面截图、console 日志。
- 入口验证：从卡券中心进入“门店养护2选1兑换券”，点击“先选门店”后进入省市门店选择步骤。
- 展示内容：车辆卡片展示“日产轩逸 2024款”、车牌“粤B·3X52M”和 `VIN：LGBH52E02RS019375`，并显示“不可更改”及券已发放至该 VIN 的说明。
- 只读验证：车辆卡片内部 `input/select/textarea/button` 数量为 0，不存在选车、换车或编辑入口。
- 状态保持：选择“广东省”触发页面重新渲染后，原车型、车牌和 VIN 仍完整展示。
- 视觉与运行：选门店页车辆卡片与门店表单层级清晰；浏览器 warning/error 为空。
- 连续失败次数：0。
- 结论：pass。

## 2026-07-29 商品上传与模板下载弹窗验证

- 步骤：Step 18 商品上传与模板下载弹窗。
- 验证工具：Node 语法检查、本地 HTTP 预览、浏览器 DOM 与实际点击、console 日志。
- 上传入口：点击“上传文件”打开批量导入弹窗，展示整券履约方式、兑换方式、当前已选商品数，并支持“追加商品”和“覆盖当前商品”两种导入方式。
- 文件约束：弹窗明确仅支持 `.xlsx`、不超过 5MB、读取第一个工作表、最多 5,000 行；未选择文件时禁止进入有效校验流程。
- 导入字段：N选M场景展示 `SKU编码`、`商品名称`、`单商品最多可兑换数量`、`供应商编码`，其中SKU编码与单商品最多可兑换数量必填。
- 校验模拟：选择示例文件后展示3行校验结果，其中2行通过、1行因商品不支持“车联网履约”失败；仅允许导入校验通过的数据。
- 模板下载：点击“下载模板”打开模板说明弹窗，展示版本 `V2026.07`、当前整券履约方式、兑换方式、字段规则和Excel示例行；点击“下载 Excel 模板”可完成模拟下载。
- 单一履约边界：履约方式不进入Excel模板，导入时统一使用当前整券履约方式校验SKU；切换兑换方式后需重新下载模板，数量字段会随兑换方式变化。
- 运行结果：页面脚本语法通过；浏览器 warning/error 为空。
- 连续失败次数：0。
- 结论：pass。

## 2026-08-10 商品下钻到 SKU/规格验证

- 步骤：Step 19 商品下钻到 SKU/规格并回显。
- 静态验证工具：Node 语法检查、Mock 数据断言、资源版本与关键字段残留检查。
- 通过项：`admin-create.js`、`mock/data.js`、`annotations.js` 语法通过；车联网商品池包含 7 个SPU、9个SKU，其中车机流量包和智能座舱主题皮肤各包含2个规格；所有车联网SKU均具备 `spuName` 与 `specName`。
- 通过项：页面源码包含商品层分页、“全选本页全部规格”、“选择规格 / SKU”、逐SKU数量输入、“确认规格”以及主表单商品名/规格/SKU编码回显。
- 通过项：上传文件和下载模板仍按SKU编码工作，整券单一履约口径未改变；静态资源版本已更新为 `coupon-20260810-1`。
- 待验证项：本地浏览器自动检查被本地地址安全策略阻断，未取得本轮实际点击、截图及console新鲜证据。
- 连续失败次数：1（验证环境限制，不是已确认的页面逻辑失败）。
- 结论：fail；实现与静态检查已完成，实际交互验证待用户刷新本地页面后继续。

## 2026-08-10 单文件静态原型构建验证

- 步骤：Step 20 生成可直接打开的单文件静态原型。
- 验证工具：Node构建与内嵌脚本语法检查、外部资源残留扫描、文件类型与摘要检查。
- 构建产物：`outputs/原型/通用兑换券静态原型_20260810.html`，UTF-8 HTML，约184KB。
- 通过项：全量CSS、Mock、导航、21段页面/交互脚本及标注运行时均已内嵌；外部CSS/JS引用为0；导航JSON请求为0。
- 通过项：21段内嵌脚本均通过语法编译；产物包含SKU/规格下钻相关Mock与页面代码。
- 文件摘要：SHA-256 `9fb83297e99a1b5ac496fd126d7957d5ac25db2f44b54fdc866aedd0067f9596`。
- 限制：受本地文件浏览器策略影响，本轮未补做单文件版视觉与实际点击验收；不影响其无外部依赖的静态交付属性。
- 连续失败次数：0。
- 结论：pass（单文件构建与依赖检查）。
